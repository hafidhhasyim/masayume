##############################################
# Environment variables for Next.js (Vercel)
##############################################

# Turso / libsql database connection
TURSO_CONNECTION_URL="libsql://<your-db-name>.turso.io"
TURSO_AUTH_TOKEN="<your-turso-auth-token>"

# Vercel Blob token for production file uploads
# Create a Blob storage in Vercel, then add a Read-Write token
# to Project Settings → Environment Variables
BLOB_READ_WRITE_TOKEN="<your-vercel-blob-rw-token>"
# Alternatively, you can use VERCEL_BLOB_RW_TOKEN as alias
# VERCEL_BLOB_RW_TOKEN="<your-vercel-blob-rw-token>"

# Notes:
# - Do NOT commit your real .env values; use this template.
# - In local dev, you can omit BLOB_READ_WRITE_TOKEN and uploads
#   will be stored in `public/uploads`.
# - In production (Vercel), the filesystem is read-only; provide
#   BLOB_READ_WRITE_TOKEN so uploads use Vercel Blob.